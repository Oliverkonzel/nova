nova-voice-agent/
│
├── START_HERE.txt          ← READ THIS FIRST!
├── QUICKSTART.md           ← 15-minute setup guide
├── README.md               ← Full documentation
│
├── .env                    ← Your API keys (all configured!)
├── .gitignore              ← Git ignore file
├── requirements.txt        ← Python packages to install
│
└── backend/                ← Main application code
    │
    ├── main.py             ← START SERVER HERE: python main.py
    ├── config.py           ← Loads API keys from .env
    ├── models.py           ← Data structures (CallData, etc.)
    │
    ├── routes/             ← API endpoints
    │   ├── health.py       ← Health check: GET /health
    │   └── webhooks.py     ← Twilio webhooks (MOST IMPORTANT!)
    │                          - /voice/incoming (answers calls)
    │                          - /voice/process (handles speech)
    │                          - /voice/book (books appointments)
    │
    └── services/           ← Business logic
        ├── conversation.py ← OpenAI integration (AI conversation)
        ├── calendar.py     ← Cal.com integration (booking)
        ├── sms.py          ← Twilio SMS (text messages)
        └── crm.py          ← Notion integration (logging)


KEY FILES EXPLAINED:
====================

📄 .env
   - Contains ALL your API keys
   - Already configured and ready!
   - Never commit to git

📄 backend/main.py
   - Entry point - run this to start server
   - Command: python main.py

📄 backend/routes/webhooks.py ⭐ MOST IMPORTANT
   - Handles incoming calls from Twilio
   - Orchestrates entire conversation flow
   - START READING HERE to understand the system

📄 backend/services/conversation.py
   - Talks to OpenAI GPT-4
   - Manages conversation state
   - Extracts structured data (name, phone, etc.)

📄 backend/services/calendar.py
   - Checks Cal.com for available slots
   - Books appointments
   - Formats times for speech

📄 backend/services/sms.py
   - Sends SMS via Twilio
   - Confirmation messages

📄 backend/services/crm.py
   - Creates leads in Notion
   - Logs call data

📄 requirements.txt
   - List of Python packages needed
   - Install with: pip install -r requirements.txt


QUICK COMMANDS:
===============

Install dependencies:
  pip install -r requirements.txt

Start server:
  cd backend
  python main.py

Test health:
  curl http://localhost:8000/health

Start ngrok (in separate terminal):
  ngrok http 8000


YOUR PHONE NUMBER:
==================
+1 (814) 568-5796

Call this after setup to talk to Nova!


WHAT NOVA DOES:
===============
1. Answers calls via Twilio
2. Natural conversation via OpenAI
3. Collects: name, phone, email, service
4. Books appointments in Cal.com
5. Sends SMS confirmations
6. Logs everything to Notion


READING ORDER:
==============
1. START_HERE.txt (you might be reading this now!)
2. QUICKSTART.md (get it running)
3. README.md (understand the system)
4. backend/routes/webhooks.py (see the flow)
5. backend/services/conversation.py (AI logic)


GET STARTED:
============
→ Read QUICKSTART.md and follow the 6 steps!
